---
phase: 01-foundation-lobby
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .gitignore
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/realtime/package.json
  - apps/realtime/tsconfig.json
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
autonomous: false
requirements:
  - TECH-01
user_setup:
  - service: npm registry package verification
    why: "RESEARCH.md marked all npm packages [ASSUMED] because slopcheck could not verify npm packages in this sandbox."
    dashboard_config:
      - task: "Verify listed npm packages on npmjs.com or with npm view before allowing installation."
        location: "npm registry"
must_haves:
  truths:
    - "The repository has a strict TypeScript pnpm workspace matching the documented Bidstand app/package boundaries."
    - "The web, realtime, db, and shared packages can be installed, typechecked, and addressed with pnpm filters."
    - "Local developers have documented commands and env examples for running the full stack."
  artifacts:
    - "package.json"
    - "pnpm-workspace.yaml"
    - "tsconfig.base.json"
    - "apps/web/package.json"
    - "apps/realtime/package.json"
    - "packages/db/package.json"
    - "packages/shared/package.json"
  key_links:
    - "Workspace scripts route pnpm typecheck and lint commands to the package graph."
    - "Package names match the filter commands documented in AGENTS.md."
    - "Each package extends the root strict TypeScript configuration."
---

## Phase Goal

**As a** Commissioner and room participant, **I want to** create and join a persisted auction room lobby, **so that** teams can gather before the live auction starts.

<objective>
Create the Bidstand walking-skeleton workspace foundation.

Purpose: later plans need real package boundaries before they can add Prisma, API routes, UI routes, and the realtime server.
Output: pnpm workspace files, package manifests, strict TypeScript configuration, and verified dependency-install gate.
</objective>

<execution_context>
@/home/varad/.codex/gsd-core/workflows/execute-plan.md
@/home/varad/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-lobby/01-RESEARCH.md
@.planning/phases/01-foundation-lobby/01-PATTERNS.md
@.planning/phases/01-foundation-lobby/01-SKELETON.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm packages before installation</name>
  <read_first>
    - .planning/phases/01-foundation-lobby/01-RESEARCH.md
  </read_first>
  <files>.planning/phases/01-foundation-lobby/01-RESEARCH.md</files>
  <action>Before running any package install, verify these npm package names and registry pages: next, react, react-dom, typescript, zod, prisma, @prisma/client, next-auth, @auth/core, bcryptjs, jose, express, socket.io, socket.io-client, cors, dotenv, tsx, vitest, tailwindcss, lucide-react. This checkpoint is required because the package legitimacy audit marked them [ASSUMED]. Do not install packages until the user confirms the names are legitimate.</action>
  <verify>
    <automated>npm view next version && npm view react version && npm view zod version && npm view prisma version && npm view socket.io version</automated>
  </verify>
  <acceptance_criteria>
    - User has confirmed package legitimacy, or the automated npm view command returns versions for the listed representative packages.
    - No package marked [SLOP] or suspicious by the user is installed.
  </acceptance_criteria>
  <done>Package installation is approved with no unresolved [ASSUMED] package risk.</done>
</task>

<task type="auto">
  <name>Task 2: Create strict pnpm workspace scaffold</name>
  <read_first>
    - AGENTS.md
    - docs/ARCHITECTURE.md
    - .planning/phases/01-foundation-lobby/01-SKELETON.md
  </read_first>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json, .gitignore, apps/web/package.json, apps/web/tsconfig.json, apps/realtime/package.json, apps/realtime/tsconfig.json, packages/db/package.json, packages/db/tsconfig.json, packages/shared/package.json, packages/shared/tsconfig.json</files>
  <action>Create the monorepo directories `apps/web`, `apps/realtime`, `packages/db`, and `packages/shared`. Add root scripts named `typecheck`, `lint`, `dev:web`, `dev:realtime`, and `db:migrate`. Use package names `@bidstand/web`, `@bidstand/realtime`, `@bidstand/db`, and `@bidstand/shared` so the documented pnpm filter commands work. Configure strict TypeScript from `tsconfig.base.json` and have every package extend it. Add `.gitignore` entries for `node_modules`, `.next`, `dist`, generated Prisma client output if applicable, and all `.env` files except `.env.example`.</action>
  <verify>
    <automated>pnpm --filter @bidstand/shared typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` includes `apps/*` and `packages/*`.
    - Root `package.json` contains scripts `typecheck`, `lint`, `dev:web`, `dev:realtime`, and `db:migrate`.
    - Every package has a `tsconfig.json` with `extends` pointing to the root strict config.
    - No file contains TypeScript `any` types introduced by scaffold code.
  </acceptance_criteria>
  <done>The workspace installs and package filter names match AGENTS.md setup commands.</done>
</task>

<task type="auto">
  <name>Task 3: Add local environment examples and run documentation</name>
  <read_first>
    - AGENTS.md
    - docs/ARCHITECTURE.md
    - .planning/PROJECT.md
  </read_first>
  <files>apps/web/.env.example, apps/realtime/.env.example, README.md</files>
  <action>Create `apps/web/.env.example` with `DATABASE_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `NEXT_PUBLIC_REALTIME_URL`, and `ROOM_JWT_SECRET`. Create `apps/realtime/.env.example` with `DATABASE_URL`, `ROOM_JWT_SECRET`, `ALLOWED_ORIGIN`, and `PORT`. Add a concise README setup section with the exact commands from AGENTS.md and note that `ROOM_JWT_SECRET` must match in both app env files.</action>
  <verify>
    <automated>test -f apps/web/.env.example && test -f apps/realtime/.env.example && grep -q "ROOM_JWT_SECRET" apps/web/.env.example && grep -q "ROOM_JWT_SECRET" apps/realtime/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - Web env example contains all five required keys with non-secret example values only.
    - Realtime env example contains all four required keys with non-secret example values only.
    - README contains commands for `pnpm install`, Prisma migration, web dev, realtime dev, `pnpm typecheck`, and `pnpm lint`.
  </acceptance_criteria>
  <done>Local setup is documented and no secret values are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> package registry | Dependency names from planning artifacts become install commands. |
| repo -> local env | `.env.example` is committed while real `.env` files remain local secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-SC | Tampering | npm package install | mitigate | Block package installation behind the package legitimacy checkpoint because RESEARCH.md marked all packages [ASSUMED]. |
| T-01-01-I | Information Disclosure | `.env` handling | mitigate | Commit only `.env.example` and ignore real `.env` files. |
| T-01-01-T | Tampering | workspace scripts | mitigate | Use exact package names and pnpm filters from AGENTS.md so commands target intended packages. |
</threat_model>

## Artifacts this phase produces

- `package.json`
- `pnpm-workspace.yaml`
- `tsconfig.base.json`
- `.gitignore`
- `apps/web/package.json`
- `apps/web/tsconfig.json`
- `apps/web/.env.example`
- `apps/realtime/package.json`
- `apps/realtime/tsconfig.json`
- `apps/realtime/.env.example`
- `packages/db/package.json`
- `packages/db/tsconfig.json`
- `packages/shared/package.json`
- `packages/shared/tsconfig.json`
- README setup section

<verification>
- `pnpm --filter @bidstand/shared typecheck`
- `test -f apps/web/.env.example && test -f apps/realtime/.env.example`
- `grep -q "ROOM_JWT_SECRET" apps/web/.env.example`
- `grep -q "ROOM_JWT_SECRET" apps/realtime/.env.example`
</verification>

<success_criteria>
- Workspace package names and scripts match AGENTS.md setup commands.
- Strict TypeScript configuration is shared by all packages.
- Env examples document all required secrets without committing real secrets.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-lobby/01-01-SUMMARY.md` when done.
</output>
