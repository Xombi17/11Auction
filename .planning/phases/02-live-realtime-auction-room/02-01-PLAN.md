---
phase: 02-live-realtime-auction-room
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/rooms.ts
  - packages/shared/src/index.ts
  - apps/realtime/src/server.ts
  - apps/realtime/src/lobby.ts
  - apps/web/app/room/[code]/lobby/page.tsx
files_created:
  - apps/realtime/src/rooms/auction.ts
  - apps/realtime/src/rooms/timer.ts
  - apps/realtime/src/rooms/auction.test.ts
  - apps/web/app/room/[code]/page.tsx
autonomous: false
requirements:
  - ROOM-04
  - ROOM-05
  - PLAY-01
  - PLAY-02
  - BID-01
  - BID-02
  - BID-03
  - TECH-02
must_haves:
  truths:
    - "The Socket.io server is the absolute authority for validating and accepting bids."
    - "Bids are processed sequentially to guarantee consistency and avoid concurrency races."
    - "Every valid bid resets the server-side countdown timer."
    - "Timer expiry resolves the active item as SOLD to the highest bidder or UNSOLD."
    - "Commissioner can pause and resume the timer, and force-resolve items."
    - "Client UI converges instantly across all open browser profiles/sessions."
  artifacts:
    - "apps/realtime/src/rooms/auction.ts"
    - "apps/realtime/src/rooms/timer.ts"
    - "apps/web/app/room/[code]/page.tsx"
  key_links:
    - "Web client connects to the socket server using the room-scoped JWT token."
    - "The room lobby redirects users to the live auction room if status is AUCTION or PAUSED."
---

## Phase Goal

**As a** room participant, **I want to** place bids and see them update in real-time, and **as a** Commissioner, **I want to** control the auction progression, **so that** the live auction resolves fairly and reliably.

<objective>
Implement the server-authoritative live bidding engine, active timer managers, database synchronization logic, and live auction room client views.
</objective>

<execution_context>
@/home/varad/.codex/gsd-core/workflows/execute-plan.md
@/home/varad/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-live-realtime-auction-room/02-RESEARCH.md
@.planning/phases/02-live-realtime-auction-room/02-UI-SPEC.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define socket contract schemas and types</name>
  <read_first>
    - packages/shared/src/rooms.ts
  </read_first>
  <files>packages/shared/src/rooms.ts</files>
  <action>Add validation schemas and types for `bid:place`, `room:start`, `room:pause`, `room:resume`, `room:force-resolve` to `packages/shared/src/rooms.ts` and export them from `packages/shared/src/index.ts`.</action>
  <verify>
    <automated>pnpm --filter @bidstand/shared typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/rooms.ts` defines schemas for socket events.
    - `pnpm --filter @bidstand/shared typecheck` succeeds.
  </acceptance_criteria>
  <done>Socket contract types and Zod schemas are defined and compile successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bidding engine state machine</name>
  <read_first>
    - packages/db/prisma/schema.prisma
  </read_first>
  <files>apps/realtime/src/rooms/auction.ts</files>
  <action>Create `apps/realtime/src/rooms/auction.ts`. Implement the `handleAuctionConnection` function, including bid validation rules (purse check, squad size, increment, role quota, and absolute timestamp check). Validate all incoming payload parameters with Zod schemas. On valid bid, save to DB and broadcast the updated state.</action>
  <verify>
    <automated>pnpm --filter @bidstand/realtime typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/realtime/src/rooms/auction.ts` contains functions to process bids, start, pause, and force-resolve auctions.
    - Code references only Zod-validated payloads.
  </acceptance_criteria>
  <done>Bidding engine logic and DB transactions are fully implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement server-side timer manager</name>
  <read_first>
    - packages/db/prisma/schema.prisma
  </read_first>
  <files>apps/realtime/src/rooms/timer.ts</files>
  <action>Create `apps/realtime/src/rooms/timer.ts`. Manage active timeouts per room using server memory. On expiry, query current high bid, resolve player as SOLD or UNSOLD in Postgres, then automatically advance to the next pending player or mark the room as COMPLETED.</action>
  <verify>
    <automated>pnpm --filter @bidstand/realtime typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/realtime/src/rooms/timer.ts` exposes functions to start, reset, pause, and clear countdown timers per room.
    - Timer expiry triggers correct database updates.
  </acceptance_criteria>
  <done>Countdown timer managers are fully implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Wire auction socket handling into the server</name>
  <read_first>
    - apps/realtime/src/server.ts
  </read_first>
  <files>apps/realtime/src/server.ts</files>
  <action>Modify `apps/realtime/src/server.ts` to attach the live auction socket connection events. Ensure rooms that were active on server startup are recovered gracefully (e.g. put in PAUSED state).</action>
  <verify>
    <automated>pnpm --filter @bidstand/realtime typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Socket connection handler wires in `handleAuctionConnection`.
    - Server starts and compiles.
  </acceptance_criteria>
  <done>Realtime socket listeners and recovery mechanics are wired.</done>
</task>

<task type="auto">
  <name>Task 5: Update web client routing transitions</name>
  <read_first>
    - apps/web/app/room/[code]/lobby/page.tsx
  </read_first>
  <files>apps/web/app/room/[code]/lobby/page.tsx</files>
  <action>Modify `apps/web/app/room/[code]/lobby/page.tsx` to redirect the client to `/room/[code]` if the room status transitions to `AUCTION` or `PAUSED`. Update the "Start Auction" button click handler to emit `room:start` via the socket.</action>
  <verify>
    <automated>pnpm --filter @bidstand/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The lobby page redirects when status is `AUCTION` or `PAUSED`.
    - Start Auction button emits `room:start`.
  </acceptance_criteria>
  <done>Lobby screen transitions are configured.</done>
</task>

<task type="auto">
  <name>Task 6: Create live auction room screen</name>
  <read_first>
    - docs/DESIGN_SYSTEM.md
  </read_first>
  <files>apps/web/app/room/[code]/page.tsx</files>
  <action>Create the live auction view at `apps/web/app/room/[code]/page.tsx`. Implement the 3-column layout matching visual specifications: Team Purse Strip at the top, Current Player Card + Bidding Buttons and Bid History in the center, and Commissioner Console at the bottom (checked server-side and visible only to Commissioners). Redirect to lobby if status changes to LOBBY, or to results page if COMPLETED.</action>
  <verify>
    <automated>pnpm --filter @bidstand/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/room/[code]/page.tsx` contains the active player display, bid buttons, countdown visualizer, and admin controls.
    - The client page loads, compiles, and communicates with the socket server.
  </acceptance_criteria>
  <done>Live auction screen is fully implemented.</done>
</task>

<task type="auto">
  <name>Task 7: Write unit tests for bidding engine</name>
  <read_first>
    - apps/realtime/src/rooms/auction.ts
  </read_first>
  <files>apps/realtime/src/rooms/auction.test.ts</files>
  <action>Create unit tests in `apps/realtime/src/rooms/auction.test.ts` to test bid validations, timer resets, and outcome resolutions in isolation from socket transport.</action>
  <verify>
    <automated>pnpm --filter @bidstand/realtime test</automated>
  </verify>
  <acceptance_criteria>
    - Bidding validator tests exit 0.
  </acceptance_criteria>
  <done>Unit tests pass successfully.</done>
</task>

</tasks>
